[project]
name = "foxypack"
version = "0.1.0"
description = "foxy interface library, entity balancing core, and shared parser"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[dependency-groups]
dev = [
    "mypy>=2.1.0",
    "ruff>=0.15.20",
]
